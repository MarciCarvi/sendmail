<IfModule mod_rewrite.c>
    RewriteEngine on

    # Se il file esiste già in public/ (asset statici), servilo direttamente
    RewriteRule ^public/ - [L]

    # Per tutto il resto vai direttamente a public/index.php (preserva il metodo POST)
    RewriteRule ^ public/index.php [L,QSA]
</IfModule>
